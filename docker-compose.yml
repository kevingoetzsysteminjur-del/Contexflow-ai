services:
  postgres:
    image: postgres:16
    container_name: contexflow_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: contexflow_db
      POSTGRES_USER: contexflow
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
